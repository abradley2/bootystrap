class HumbleNavlink

  constructor: (params) ->
    @linkLable = 'Test Link'
    @linkDestination = '#testLink'
    return

  initComponent: (view) ->

    return

  dispose: ->

    return

module.exports = HumbleNavlink
